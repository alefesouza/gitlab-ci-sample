test('adds 1 + 2 to equal 3', () => {
  expect(1 + 2).toBe(3);
});

test('adds 2 + 3 to equal 5', () => {
  expect(2 + 3).toBe(5);
});

test('adds 3 + 4 to equal 7', () => {
  expect(3 + 4).toBe(7);
});
